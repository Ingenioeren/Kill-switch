# Kill-switch
Windows network kill switch for quickly cutting connectivity, with configurable global hotkeys, selectable network adapters, system tray integration, and optional automatic process termination when the connection drops or is manually disabled.
